## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- What problem does it solve, or what question does it answer? Link an issue if there is one. -->

## How it was checked

<!-- Be specific. "Ran it" is not checking. For example:
     - ran `python code/classical_baseline.py --subject 1 --band 8 13` on the cluster, job 81619
     - numbers before / after, with the chance level stated
     - or: docs only, no code paths touched -->

## Results, if this produced any

<!-- Paste the numbers. Always state the chance level and the sample size next to an
     accuracy, and say whether it is a validation or a test figure. A number without
     its baseline is not a result. -->

## Checklist

- [ ] No data, model weights, or run outputs committed (`datasets/`, `results/`, `pretrains/models/` are gitignored for a reason)
- [ ] No credentials, tokens, or cluster paths containing anything private
- [ ] Branch is named `<yourname>/<topic>`
- [ ] If this changes a result we have published in the documentation, the documentation is updated too
